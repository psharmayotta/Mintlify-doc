---
title: "Benchmarking"
icon: "arrow-down-arrow-up"
---

Compare AI models or hardware configurations to measure performance, accuracy, and efficiency on Q0

The **Benchmarking** module lets users systematically evaluate and compare AI models — or the hardware they run on — using consistent datasets and metrics. Instead of relying on published leaderboard numbers or anecdotal testing in the Playground, Benchmarking gives users a repeatable, configurable test run on Q0's own infrastructure, so decisions about which model (or which GPU) to use in production are backed by real, side-by-side data. 

This module is designed for users who need to answer questions like-

- Which of these two models gives better quality for my use case at acceptable cost?
- Does this model perform differently on H100 vs L40S GPUs? or
- How well does my RAG pipeline perform against my own knowledge base?

**What Benchmarking Helps Users Do-**

- **Compare Models Objectively**: Run two (or more) models — from different sources, providers, or checkpoints — against the same dataset and metrics to see how they really stack up.
- **Compare Hardware Configurations**: Evaluate inference performance (latency, throughput, cost) of the same model across different GPU types and counts, to choose the most cost-effective deployment setup.
- **Evaluate a Single Model**: Run standardized NLP benchmarks against one model to understand its baseline capabilities on specific tasks.
- **Validate RAG/Knowledge Base Pipelines**: Test how well a Knowledge Base pipeline retrieves and generates answers, before relying on it in production.
- **Make Data-Driven Decisions**: Get quantified, side-by-side comparison results (covered in detail in a later section) instead of guessing which model or hardware setup is "good enough."
- **Reuse and Track Runs**: Every benchmarking run is saved with its configuration, status, and results, so users can revisit, compare across runs, and avoid re-running identical tests.

The main Benchmarking page lists every benchmarking run created by the user or team, along with its type, status, and key details.

## Create Benchmarking

The main Benchmarking page lists every benchmarking run created by the user or team, along with its type, status, and key details.

**Create Benchmarking** opens a guided, multi-step wizard. Each step is represented as a numbered, expandable card on the left, with contextual help text on the right explaining every field for that step.

Following are the Benchmarking Type User can use -

<Columns cols={2}>
  <Column>
    > **Model Benchmarking**

    Compares performance between two different AI models — using the same or different model sources — across selected metrics like accuracy, latency, and cost.
  </Column>
  <Column>
    > **Hardware Benchmarking:**

    Compares inference performance of a model across different hardware configurations and GPU counts, helping identify the most efficient/cost-effective setup.
  </Column>
</Columns>

<Columns cols={2}>
  <Column>
    > **Model Evaluation**
    >
    > Evaluates a single model on specific NLP tasks using standard benchmarks, without a second model for comparison.
  </Column>
  <Column>
    > **Knowledge Base Pipeline**
    >
    > Benchmarks the performance of a Retrieval-Augmented Generation (RAG) pipeline against a connected Knowledge Base.
  </Column>
</Columns>

## Model to Model Benchmarking

#### Provide Basic Details

Add Benchmarking Name and Description to identify this benchmarking experiment

#### Set Benchmarking Type

Select “**Model Benchmarking”,** Compares performance between two different AI models — using the same or different model sources — across selected metrics like accuracy, latency, and cost.

**Model Category:** The type of model to benchmark, based on task or modality (e.g., `Diffusion Model` for text-to-image, or text/vision/audio models). This determines which model classes and configuration fields appear later.

<Frame>
  ![Image](/images/image-46.png)
</Frame>

**Configuring Model 1 and Model 2**

·       **Model Source:** Where the model comes from — e.g., `Huggingface` (external repository) or a cloud provider such as `GCP` (internal/cloud-hosted model). |

·       **Model Path:** The repository path or storage location where the model is located. For Huggingface, this is the repo path (e.g., `moonshotai/K****struct`); for cloud sources like GCP, it's typically a storage URI (e.g., `gs://inferen****truct/`). Q0 verifies the path automatically, shown with a green **Verified** badge once confirmed.

·       **Model Class**: The specific model architecture/pipeline class used to load and run the model (e.g., `StableDiffusion3Pipeline`). This must match the model's actual implementation for the benchmark to run correctly.

<Frame>
  ![Image](/images/image-47.png)
</Frame>

·       **Secret**: Authentication credentials required to access the model source. Choose between: **Existing Secret** — select a previously saved credential from the dropdown (e.g., a saved Huggingface token or GCP service account). Create **New Secret** — add a new credential on the fly if one doesn't already exist.

·       **Select Secret**: Dropdown to pick the specific saved secret to use (e.g., `Hugg`, `GCPtest`), scoped to the chosen Model Source.

<Tip>
  **How to select Model Source, Path, and Class**: 

  First choose the **Model Source** (where the model lives — Huggingface, GCP, etc.). This determines the expected format of **Model Path** (a repo ID for Huggingface, a storage URI for cloud providers). Then select the matching **Model Class** that corresponds to the model's architecture/pipeline. Finally, attach a **Secret** so Q0 can authenticate and pull the model — Q0 verifies the path automatically and marks it **Verified** once access is confirmed. 

  This same Model Source / Model Path / Model Class / Secret block is repeated for **Model 2**, allowing two independently-sourced models (even from different providers, as shown with Model 1 on Huggingface and Model 2 on GCP) to be benchmarked against each other.
</Tip>

#### **Dataset**

This step selects or creates the data used to run and evaluate the benchmark

**Existing Dataset**: Use a dataset that's already available, either from Q0's central library or one your team has previously created/uploaded.

<Frame>
  ![Image](/images/image-48.png)
</Frame>

Selecting this reveals a ‘**Select An Existing Dataset**’ dropdown with a search bar, split into two tabs: “**Dataset Library**” — globally available, pre-curated datasets managed in Q0's central library (CMS), e.g., `DrawBench`, \`COCO

“Custom Dataset” — datasets that you or your team have already created/uploaded to Q0.

·       **Create New Dataset** : Lets the user upload or define a new dataset specifically for this benchmarking run, instead of reusing an existing one.

·       **Sample Dataset**: A reference/test dataset provided by Q0, available via a ‘**Download Sample Dataset’** link, useful for understanding the expected dataset format before creating a custom one.

 In short: pick ‘**Existing Dataset**’ to reuse something already available (library or custom), or ‘**Create New Dataset**’ to bring in fresh data tailored to this specific benchmark.

#### **Inferencing Settings**

This step configures how each model generates output during the benchmark run. Metrics are automatically enabled based on the benchmarking goal, and settings are configured independently for **Model 1** and **Model 2**

<Frame>
  ![Image](/images/image-49.png)
</Frame>

·       **No of Images**: (For image/diffusion models) The number of images each model should generate per dataset prompt during the benchmark, adjustable via slider or input (e.g., `3`, within a `1–4` range).

·       **Show Advanced Options:** Expands additional generation parameters per model, including:

o   T**emperature:** Controls randomness of output. Higher values make outputs more creative; lower values make them more deterministic and consistent.

o   **Top\_p:**  Limits token selection to a probability mass, balancing output diversity against coherence.

o   **Max Tokens:** Sets the maximum length of the generated output.

o   **Stop Sequences:** Defines specific tokens or phrases at which the model should stop generating further output.

o   **Streaming:** Enables real-time, token-by-token output generation instead of waiting for the full response.

o   **Seed:** A fixed value that ensures reproducible outputs across multiple runs, useful for fair, repeatable comparisons.

<Note>
  These advanced settings mirror the same generation controls available in the Playground (see \[Playground\](/playground) documentation) but applied consistently across the entire benchmark dataset for each model.
</Note>

#### **Hardware Configuration**

Choose the hardware or compute resources used to run the benchmark.

 

<Frame>
  ![Image](/images/image-50.png)
</Frame>

·       **GPU Type**: Selects the GPU hardware to run the benchmark on. Each option shows its specs and hourly cost, e.g.: H100 — 8 CPUs, 80.00GB HBM3, ₹432.00/GPU/hr , L40S — 4 CPUs, 48.00GB GDDR6 ECC, ₹190.80/GPU/hr Higher-end GPUs (like H100) offer better performance at a higher cost.

·       **GPU Count Per Node**: The number of GPUs to allocate per node for the benchmark run (selectable from `1` to `8`), relevant for distributed inference and for ‘**Hardware Benchmarking**’ runs that specifically compare different GPU counts.

 

Once we start creating the Benchmarking, Benchmarking Status start showing the

<Frame>
  ![Image](/images/image-51.png)
</Frame>

Each run progresses through a fixed sequence of stages, shown as a horizontal progress tracker:

·       **Verifying Model:** Q0 confirms that both models (and their credentials/paths) are valid and accessible.

·       **Model Deploy**: The selected model(s) are provisioned and deployed onto the chosen GPU hardware.

·       **Dataset Verify**: The selected dataset is validated and prepared for use in the benchmark run.

·       **Benchmarking:** The actual test run executes — both models are run against the dataset and the configured inferencing settings, and metrics are collected.

·       **Benchmark Completed:** The run has finished; results and comparison data are now available. 

Each stage is marked ‘**In progress**’, ‘**Pending**’, or ‘**Completed**’ in real time, so users can track exactly where their run is in the pipeline without needing to refresh or guess.

Once Benchmarking is completed and is in Success state then following Metrices are created-

For **Model Benchmarking** runs on Diffusion Models (Text to Image), Q0 generates a side-by-side comparison of the two models across an **Overview** tab and one tab per dataset used — for example, **DrawBench** and **COCO**. Unlike the LLM-oriented Task Metrics described above, image generation comparisons surface image-specific Quality Metrics (FID, CLIP Score, Human Preference) alongside Performance and Reliability data.

**How Results Are Labeled**

| Column | Example | Description |
| --- | --- | --- |
| Model 1 | \`Gpt-Oss-20b\` ⭐ | The first model in the comparison. Marked with a star (⭐) if it is the better overall performer across the run. |
| Model 2 | \`Stable-Diffusion-3.5-Large\` | The second model in the comparison. |

**Result Navigation Tabs**

| Tab | Description |
| --- | --- |
| Overview | Aggregated results across all datasets used in the run. In this example it mirrors the DrawBench Quality Metrics, since DrawBench is the primary quality-scoring dataset. |
| DrawBench | Dataset-specific results from the DrawBench dataset — a prompt suite designed to stress-test text-to-image models across categories like color accuracy and object counting. |
| COCO | Dataset-specific results from the COCO dataset — a general-purpose image benchmark used to score realism and semantic alignment via FID and CLIP Score. |

**1. Quality Metrics — DrawBench**

| Metric | Gpt-Oss-20b (Model 1) | Stable-Diffusion-3.5-Large (Model 2) | Description |
| --- | --- | --- | --- |
| Success Rate | • 50.00% | • 50.00% | Percentage of DrawBench prompts each model successfully generated an image for, without errors or timeouts. |
| Overall Score | • 39.47 | • 39.47 | A composite quality score aggregating all DrawBench category scores into a single number for the model. |
| Category Scores – Colors | • 78.95 | • 78.95 | How accurately the model rendered the specific colors requested in the prompt (e.g., "a red cube next to a blue sphere"). |
| Category Scores – Counting | • 0 | • 0 | How accurately the model rendered the correct number of objects requested in the prompt (e.g., "three apples on a table"). |
| Human Preference | • 50 | • 50 | A relative preference score based on (simulated or collected) human judgments comparing outputs from both models on the same prompts. |
| Generation Quality | • estimated fair | • estimated fair | An overall qualitative rating of image generation quality, estimated from the combined DrawBench category scores. |

<Note>
  **DrawBench** includes additional prompt categories beyond Colors and Counting (e.g., Position, Text Rendering, Conflicting Prompts) in the full benchmark suite. Only categories with non-zero coverage for the current run are shown here — the specific categories displayed depend on which DrawBench prompt subsets were included in the dataset selection.
</Note>

**2. Quality Metrics — COCO**

| Metric | Gpt-Oss-20b (Model 1) | Stable-Diffusion-3.5-Large (Model 2) | Description |
| --- | --- | --- | --- |
| FID (Fréchet Inception Distance) | • 100 | • 100 | Measures how close the distribution of generated images is to the distribution of real COCO images. Lower values normally indicate more realistic, higher-fidelity generations. |
| CLIP Score | • 0 | • 0 | Measures how well each generated image semantically matches its text prompt, using CLIP embeddings to score image-text alignment. |
| Success Rate | • 0.00% | • 0.00% | Percentage of COCO prompts each model successfully generated an image for. |
| Inception Score | • 1 | • 1 | A measure of both the quality and diversity of generated images, based on how confidently and variably an Inception classifier labels them. |
| Latency Ms – P95 | • 0.00 ms | • 0.00 ms | 95th percentile generation latency for COCO prompts specifically. |
| Latency Ms – P99 | • 0.00 ms | • 0.00 ms | 99th percentile generation latency for COCO prompts specifically. |

<Tip>
  **Reading FID and CLIP Score together**:FID captures overall image realism/fidelity against real photos, while CLIP Score captures prompt faithfulness. A model can score well on one and poorly on the other — for example, generating photorealistic but semantically unrelated images. Always check both metrics together rather than optimizing for just one.
</Tip>

**3. Performance Metrics**

| Metric | Description |
| --- | --- |
| Latency Ms – P95 | 95th percentile time to generate a single image, across all prompts in the run. Lower is better. |
| Latency Ms – P99 | 99th percentile image generation latency — reflects near-worst-case generation time. Lower is better. |
| Latency Ms – Mean | Average time to generate a single image across all prompts. Lower is better. |
| Latency Ms – Median | Median (P50) image generation latency — less sensitive to outliers than the mean. Lower is better. |
| Throughput – Images Per Hour | The sustained number of images each model can generate per hour under the benchmark's hardware and concurrency settings. Higher is better. |
| Throughput – Images Per Minute | The sustained number of images generated per minute. Higher is better. |
| Throughput – Pixels Per Second | Total pixel volume generated per second — accounts for both generation speed and output resolution. Higher is better. |

**4. Image Specs & Reliability**

| Metric | Description |
| --- | --- |
| Image Specs – Average Resolution | The average total pixel count (width × height) of images generated by each model (e.g., 1024×1024 = 10,48,576 pixels). Equal values confirm both models were configured to generate at the same output resolution. |
| Image Specs – Total Pixels Generated | The cumulative pixel count across every image generated during the run, for each model. |
| Reliability – Success Rate | Percentage of all generation requests (across both datasets) that completed successfully. |
| Reliability – Total Samples | Total number of prompts submitted to each model across the full run. |
| Reliability – Failed Samples | Number of prompts that failed to generate a valid image. |
| Reliability – Successful Samples | Number of prompts that successfully generated a valid image. |
| Total Time Minutes | Total wall-clock time taken to complete the full benchmark run, in minutes. Lower is better. |
| Total Time Seconds | Total wall-clock time taken to complete the full benchmark run, in seconds. Lower is better. |

<Note>
  **Reading this example**:

  With a 50% Reliability Success Rate and 15 of 30 samples failing, this run should be treated as a partial/test result rather than a production-quality comparison — the Quality Metrics (FID, CLIP Score, Overall Score) are only computed from the successful samples. Before trusting a model comparison for a production decision, confirm the Reliability Success Rate is close to 100% first; a low success rate can distort quality scores by silently excluding harder prompts that failed to generate.
</Note>

## **Hardware to Hardware Benchmarking**

**Hardware Benchmarking** compares how the same model performs across two different hardware setups — different GPU types or different GPU counts — under identical workloads. This is the right benchmarking type when the question is not _which model_ is better, but _which infrastructure_ is most performant or cost-efficient for a model you've already chosen.

#### **Basic Details**

Same as Model Benchmarking. Provide a unique **Benchmarking Name** (e.g., `Bench_Hardware`) and an optional **Description** (up to 500 characters), then click Continue.

#### **Benchmarking Type: Hardware Configuration**

Select ‘**Hardware Benchmarking**’ as the type. This reveals two independent hardware configuration blocks — **Hardware 1** and **Hardware 2** — to define the two setups being compared.

<Frame>
  ![Image](/images/image-52.png)
</Frame>

 Both blocks follow the same structure: 

- **Hardware (GPU Type)**: Select the GPU model for this configuration. Options shown with specs and hourly cost: • **H100** — 8 CPUs, 80.00GB HBM3, (high-end, highest throughput) • **L40S** — 4 CPUs, 48.00GB GDDR6 ECC, (cost-efficient, mid-range)
- **GPU Count Per Node**: Number of GPUs to allocate per node for this hardware setup.

For example: Hardware 1 with H100 × 3 GPUs vs. Hardware 2 with L40S × 2 GPUs.

- **Concurrent Users**: The number of simultaneous users/requests to simulate against each hardware setup during the benchmark.

Tests how each configuration handles load. Available values: `1`, `5`, `10`, `15`, `20`. Multiple values can be selected to test each hardware setup across several load levels in a single run — for example, `1` tests single-user latency while `10` or `20` tests throughput under realistic concurrent load.

<Card title="Concurrent Users" type="note">
  **Concurrent Users** is the key variable for Hardware Benchmarking. A setup that excels at low concurrency (1 user) may fall behind at higher load (10–20 users). Selecting multiple concurrency levels gives you a performance curve for each hardware configuration across the full range of expected traffic, not just a single data point.
</Card>

#### **Model Configuration**

Specify the single model that will be deployed on both hardware setups for comparison. The fields here are the same as in Model Benchmarking — Model Source, Model Path, Model Class, and Secret — but only one model is configured, since the variable being compared is the hardware, not the model. See \[Configuring Model 1 and Model 2\](#configuring-model-1-and-model-2) for field-level details.

<Frame>
  ![Image](/images/image-53.png)
</Frame>

#### **Select Dataset**

Select the dataset the model will run against on both hardware configurations. The same dataset options are available as in Model Benchmarking. For LLM/text model hardware benchmarking, standard NLP benchmarks from the Dataset Library are commonly used:

 

<Frame>
  ![Image](/images/image-54.png)
</Frame>

**MMLU**: Massive Multitask Language Understanding — a broad academic benchmark spanning 57 subjects including math, science, law, and history.

**HellaSwag:** A commonsense reasoning benchmark that tests how well a model can complete a sentence or scenario description in context.  

Multiple datasets can be selected simultaneously (e.g., both `MMLU` and `HellaSwag`) so each hardware configuration is evaluated across more than one task type in a single run.

#### **Inferencing Settings**

Defines how the selected model generates responses during the benchmark. Since hardware — not the model — is the variable being tested, these settings are configured once and applied identically on both hardware setups to ensure a fair comparison. The model being benchmarked is shown at the top of this step (e.g., `Llama3-1-8B`).

 

<Frame>
  ![Image](/images/image-55.png)
</Frame>

 

·       **Max Tokens**`4096` (range: 64–8192) | Maximum tokens the model can generate per response. Longer outputs stress hardware memory and compute more, making this a key variable in throughput tests.

·       **Temperature:** `0.7` (range: 0–2) | Controls output randomness. Lower values produce deterministic responses; higher values introduce variety.

·       **System Prompt:** `"Act as a helpful assistant to the user"` | Sets the model's role for all prompts in the dataset. Kept identical across both hardware setups.

·       **Top K:** `3` (range: 3–7) | Limits sampling to the top-K most probable next tokens at each step.

·       **Score Threshold:** `0` (range: 0–1) | Minimum relevance/confidence score for outputs to be included in metric calculations.

 

Clicking **Show Advanced Options** reveals additional generation controls: 

·       **Top-P**: `0.95` (range: 0–1) | Nucleus sampling — limits token candidates to a cumulative probability ≥ Top-P, balancing coherence and diversity.

·       **Frequency Penalty**`0.5` (range: -2 to 2) | Reduces repetition of tokens already used frequently in the output.

·       **Presence Penalty**`0.5` (range: -2 to 2) | Encourages the model to introduce new topics/tokens rather than repeating what's already been said.

 

After Step 5, a full configuration summary is displayed covering all settings for both hardware configurations. Review it, then click **Create Benchmarking** to launch the run. The benchmark then progresses through the same status pipeline as all other benchmarking types.

<Frame>
  ![Image](/images/image-56.png)
</Frame>

#### Hardware Benchmarking Results

Once a Hardware Benchmarking run reaches **Benchmark Completed**, Q0 generates a side-by-side comparison of the two hardware configurations across nine metric categories. Each table shows two columns — one per hardware setup — with color-coded indicators (🟢 green = better performer, 🔴 red = lower performer) making it immediately clear which hardware won on each metric.

**How Results Are Labeled**

| Column | Example | Description |
| --- | --- | --- |
| Hardware 1 | \`H100-2x-Cluster\` | The first hardware configuration — GPU type and count (e.g., H100 × 2 GPUs). Marked with ⭐ if it is the better overall performer. |
| Hardware 2 | \`L40S-1x-Cluster\` | The second hardware configuration (e.g., L40S × 1 GPU). |

<Info>
  Green dots (🟢) indicate the better value for that metric; red dots (🔴) indicate the lower value.
</Info>

**1. Request Summary**

The Request Summary provides a high-level snapshot of how each hardware configuration handled the benchmark workload — confirming request volume, token counts, and overall success.

| Metric | Description |
| --- | --- |
| Avg Latency Ms | Average time per inference request across the full dataset. Lower is better. |
| Total Requests | Total number of inference requests submitted to each hardware configuration. Should match across both setups for a fair comparison. |
| Failed Requests | Number of requests that returned an error or did not complete. \`0\` on both indicates a clean, fully successful run. |
| Total Input Tokens | Total tokens sent as input across all requests. Equal on both — confirms the same workload was applied to each hardware. |
| Successful Requests | Number of requests that returned a valid response. \`120/120\` = 100% success on both. |
| Total Output Token | Total tokens generated by the model across all responses. Minor variance between hardware setups can reflect subtle differences in how each processes and truncates output. |
| Success Rate Percent | Percentage of total requests that completed successfully. |

**2. Performance Metrics**

Performance Metrics measure the raw speed and throughput of each hardware configuration under the benchmark workload — the core indicators for production serving SLAs.

| Metric | Description |
| --- | --- |
| TTFT M (Time to First Token) | Time elapsed from when a request is submitted to when the first output token is produced. Critical for streaming/real-time applications where perceived responsiveness matters. |
| P95 Latency Ms | 95th percentile latency — 95% of all requests completed within this time. The key SLA metric for production deployments where tail latency must stay within acceptable bounds. |
| Tokens Per Second | The rate at which the hardware generates output tokens across all concurrent requests. Higher is better — reflects raw generative throughput of each hardware setup. |
| Peak Throughput RPS | The peak number of requests per second observed during the benchmark run. |
| Requests Per Second | The average sustained throughput — how many inference requests each hardware completed per second across the full benchmark. Higher is better. |

**3. Resource Metrics**

Resource Metrics show how efficiently each hardware configuration utilized its available compute and memory during the benchmark — helping users understand utilization headroom and cost efficiency.

| Metric | Description |
| --- | --- |
| GPU Memory Used (GB) | Total GPU memory consumed by the model and its inference process. Higher memory usage on H100 reflects its larger memory capacity being actively used by the model. |
| Memory Pressure Events | Number of times the system experienced GPU memory pressure (near-OOM conditions) during the benchmark. \`0\` on both indicates stable, unconstrained memory operation. |
| CPU Utilization Percent | Percentage of available CPU capacity used during the benchmark. Low CPU utilization on both setups is expected — GPU inference is GPU-bound, not CPU-bound. |
| GPU Utilization Percent | Percentage of GPU compute capacity actively in use during the benchmark. Low GPU utilization may indicate the workload was not large enough to fully saturate the GPU — or that the model is memory-bandwidth bound rather than compute-bound. |

**4. Accuracy Results**

Accuracy Results confirm that both hardware configurations produce identical model outputs — verifying that hardware differences do not affect model correctness.

| Metric | Description |
| --- | --- |
| Total Correct | Number of evaluation questions answered correctly by the model on each hardware. Equal values confirm hardware-independent model accuracy. |
| Datasets Tested | The evaluation dataset used for accuracy scoring. |
| Total Questions | Total questions in the evaluation dataset submitted to each hardware. |
| Overall Accuracy Percent | The model's overall accuracy score across all questions on each hardware. Equal values confirm the model produces the same quality outputs regardless of which hardware it runs on. |

<Note>
  **Accuracy Results** are expected to be identical across hardware configurations in a Hardware Benchmarking run, since the same model runs on both setups. Differences here would indicate a numerical precision issue introduced by the hardware or driver configuration.
</Note>

**5. Quality of Service**

Quality of Service metrics measure how reliably each hardware configuration meets service-level expectations — translating raw latency and error data into business-relevant SLA indicators.

| Metric | Description |
| --- | --- |
| SLO Compliance Rate Percent | The percentage of requests that met the configured Service Level Objective (latency threshold). A value of 95% means 95 out of 100 requests completed within the defined SLO target. |
| Error Budget Remaining Percent | The proportion of the allowable error budget that has not yet been consumed. Higher values mean more headroom before SLA breach thresholds are hit. |
| Response Time Satisfaction Percent | The percentage of requests for which users would experience satisfactory response times, based on a predefined latency satisfaction threshold. |

**6. Reliability Metrics**

Reliability Metrics validate the stability and correctness of each hardware configuration across the full benchmark workload.

| Metric | Description |
| --- | --- |
| OOM Incidents | Number of Out-of-Memory (OOM) events during the benchmark. \`0\` on both confirms neither hardware ran out of GPU memory under the test workload. |
| Error Rate Percent | Percentage of requests that returned an error. \`0.00%\` on both indicates a fully reliable run with no failures on either hardware. |
| Success Rate Percent | Percentage of requests that completed successfully. \`100.00%\` on both confirms complete reliability across the benchmark. |

**7. Scalability Metrics**

Scalability Metrics reveal how each hardware configuration handles increasing load — showing capacity headroom, startup speed, and efficiency under horizontal scaling.

| Metric | Description |
| --- | --- |
| Scale Up Time Seconds | Time taken to bring a new replica online when scaling up. \`0.00 s\` indicates pre-warmed or near-instant scaling in the benchmark environment. |
| Max Concurrent Requests | The maximum number of simultaneous inference requests each hardware configuration handled during the benchmark, as configured by the Concurrent Users setting. |
| Pod Startup Time Seconds | Time for a new pod/container to start and become ready to serve requests after provisioning. |
| Horizontal Scaling Efficiency Percent | The efficiency of adding additional replicas — how close to linear the throughput gain is when a new replica is added. \`95%\` indicates near-linear scaling efficiency on both setups. |

**8. Infrastructure Health**

Infrastructure Health metrics provide a view into the underlying system stability of each hardware configuration during the benchmark

| Metric | Description |
| --- | --- |
| GPU ECC Errors | Number of GPU Error Correcting Code (ECC) memory errors detected during the benchmark. ECC errors indicate hardware-level memory faults. \`0\` on both confirms clean, healthy GPU hardware. |
| System Load Average | Average system load (CPU queue depth) across the benchmark duration. Lower indicates a less burdened system; H100's higher core count handles the same workload at lower relative load. |
| Disk Space Available Percent | Percentage of total disk space remaining on each node. Confirms adequate storage headroom for model weights, caches, and temporary files during inference. |

**9. Advanced Analytics**

Advanced Analytics provide deeper operational insights into queuing behavior and cost efficiency of each hardware configuration.

| Metric | Description |
| --- | --- |
| Queue Wait Time Ms | Average time requests spent waiting in queue before inference began. \`0.00 ms\` indicates no queuing bottleneck — all requests were immediately processed. |
| Cost Efficiency Score | A composite score (0–100) representing how efficiently each hardware configuration converts GPU cost into inference throughput and quality. Higher is better. Helps contextualize performance results against the ₹/GPU/hr cost of each setup. |

**10. Kubernetes Metrics**

Kubernetes Metrics confirm the health of the container orchestration layer running each hardware configuration — ensuring the infrastructure itself is performing optimally.

| Metric | Description |
| --- | --- |
| Pod Scheduling Success Rate Percent | The percentage of pod scheduling requests that were successfully placed on available nodes. \`100%\` on both confirms no scheduling failures or resource contention during the benchmark. |
| Resource Request Utilization Percent | The percentage of requested CPU/memory/GPU resources that were actively utilized during the benchmark. \`85%\` indicates efficient resource allocation — close to fully using what was provisioned without waste. |

**11. Concurrent User Breakdown**

For benchmarks configured with multiple Concurrent User levels, results are also broken down per concurrency level — allowing users to see how each hardware configuration performs under light vs. heavy load independently.

| Metric | Description |
| --- | --- |
| Avg Latency Ms | Average inference latency at this specific concurrency level (e.g., 1 concurrent user). |
| Correct Answers | Number of correct answers at this concurrency level. Equal values confirm hardware-independent accuracy. |
| Total Questions | Questions evaluated at this concurrency level (a subset of the full dataset, proportioned across concurrent user levels). |
| Accuracy Percent | Model accuracy at this specific concurrency level. |

<Tip>
  **How to read Hardware Benchmarking results**: Focus first on **Avg Latency Ms** and **Tokens Per Second** in Performance Metrics — these are the clearest indicators of which hardware is faster. Then check **Resource Metrics** to understand utilization efficiency, and **Cost Efficiency Score** in Advanced Analytics to contextualize performance against hourly GPU cost. If both configurations show identical Accuracy Results, the hardware choice is purely a performance and cost decision.
</Tip>

## **Model Evaluation**

**Model Evaluation** assesses a single model's capability on a specific task using standard, widely recognized benchmarks. Unlike Model Benchmarking (which compares two models against each other), Model Evaluation is about understanding _how well a single model performs in absolute terms_ — for example, how accurately it answers factual questions, how faithfully it transcribes speech, or how realistically it generates images. Results are scored against established metrics (accuracy, BLEU, WER, FID, etc.) so teams can verify that a model meets the bar required for their use case before deploying it.

**What Model Evaluation Helps Users Do**

 

·       **Validate baseline quality** of a model before committing to a deployment or fine-tuning investment.

·       **Verify task-specific capability** using standardized tasks (Question Answering, Summarization, OCR, Speech Recognition, etc.) rather than informal testing.

·       **Generate auditable evidence** of model performance for compliance, governance, or client-facing reporting purposes.

·       **Identify weaknesses** in a model across specific sub-tasks, so fine-tuning or model selection decisions are well-informed.

·       **Compare against industry benchmarks** by running the same evaluation tasks used in published research and leaderboards.

In the Model Evaluation, for Benchmarking Type- Model Evaluation following Evaluation task can be performed

| **Model Category** | Evaluation Task | What It Measures |
| :-- | :-- | :-- |
| Large Language Model | Question Answering | Accuracy of factual and contextual answers |
| Large Language Model | Summarization | Quality and conciseness of generated summaries |
| Large Language Model | Reasoning | Logical and commonsense reasoning ability |
| Large Language Model | Code Generation | Correctness and functionality of generated code |
| Diffusion Model | Text to Image | Visual quality and prompt relevance of generated images |
| Diffusion Model | Image Editing | Precision and naturalness of text-guided image edits |
| Vision-Language Model | Visual Question Answering | Accuracy of answers about image content |
| Vision-Language Model | Image Captioning | Quality and descriptiveness of generated captions |
| Vision-Language Model | Document Understanding | Accuracy of content extraction from document images |
| Vision-Language Model | OCR | Character- and word-level text recognition accuracy |
| Automatic Speech Recognition | Speech Recognition | Transcription accuracy of spoken audio |
| Automatic Speech Recognition | Transcription | Accuracy of full audio recording transcripts |
| Text-to-Speech | TTS Quality | Naturalness, clarity, and intelligibility of synthesized speech |

#### **Basic Details**

<Frame>
  ![Image](/images/image-57.png)
</Frame>

Provide a unique **Benchmarking Name** (e.g., `Bench_Hardware`) and an optional **Description** (up to 500 characters), then click Continue.

#### Benchmarking Type & Model Category

Select **Model Evaluation** as the Benchmarking Type.

A **Model Category** dropdown then appears, defining the type of model being evaluated and determining which Evaluation Tasks become available in the next step.

<Frame>
  ![Image](/images/image-59.png)
</Frame>

**Available Model Categories:**

| **Model Category** | **Description** |  |
| --- | --- | --- |
| Large Language Model | Text-based LLMs used for language understanding, generation, reasoning, and coding tasks. |  |
| Diffusion Model | Image generation models that produce visuals from text prompts or by editing existing images. |  |
| Vision-Language Model | Multimodal models that understand both images and text — used for captioning, visual Q&A, and document understanding. |  |
| Automatic Speech Recognition | Models that transcribe or process spoken audio into written text. |  |
| Text-to-Speech | Voice synthesis models that convert written text into natural-sounding audio output. |  |

#### Evaluation Task

This is the step unique to Model Evaluation. After selecting a Model Category, the **Evaluation Task** step appears with a **Task** dropdown listing all tasks applicable to that category. Selecting a task tells Q0 what benchmark, metrics, and evaluation protocol to apply during the run.

<Frame>
  ![Image](/images/image-60.png)
</Frame>

| **Task** | **What It Evaluates** |
| --- | --- |
| Question Answering | Tests the model's ability to answer factual and contextual questions accurately. Measures how well the model retrieves and applies knowledge from its training data or a given context. |
| Summarization | Evaluates how well the model condenses long text into concise, coherent summaries. Common metrics include ROUGE scores comparing the summary to a human-written reference. |
| Reasoning | Measures logical and commonsense reasoning capabilities — how well the model can draw conclusions, follow multi-step logic, or complete analogies. |
| Code Generation | Evaluates the model's ability to produce syntactically correct and functionally accurate code from a natural-language description of a programming task. |

**Diffusion Model — Evaluation Tasks**

| Task | What It Evaluates |
| --- | --- |
| Text to Image | Generates images from text prompts and evaluates them on quality and relevance — how accurately and realistically the model renders the described content. Common metrics include FID (Fréchet Inception Distance) and CLIP Score. |
| Image Editing | Evaluates the model's ability to modify existing images based on text instructions — for example, changing an object's color, removing a background, or applying a style. Assessed on how precisely and naturally edits are applied. |

**Vision-Language Model — Evaluation Tasks**

| Task | What It Evaluates |
| --- | --- |
| Visual Question Answering | Tests the model's ability to answer questions about the content of an image — combining image understanding with language generation. Measured on answer accuracy. |
| Image Captioning | Evaluates how well the model generates descriptive, natural-language captions for images. Assessed using metrics such as BLEU, CIDEr, and METEOR against reference captions. |
| Document Understanding | Measures the model's ability to extract, interpret, and reason over content from document images (e.g., scanned forms, invoices, reports), where both visual layout and text content matter. |
| OCR | Evaluates the model's accuracy in recognizing and extracting printed or handwritten text from images. Measured primarily by character-level and word-level accuracy. |

**Automatic Speech Recognition — Evaluation Tasks**

| Task | What It Evaluates |
| --- | --- |
| Speech Recognition | Evaluates how accurately the model transcribes spoken audio to text. The primary metric is \*\*Word Error Rate (WER)\*\* — the lower the WER, the more accurately the model matches the reference transcript. |
| Transcription | Assesses the model's ability to convert full audio recordings into accurate, readable written transcripts, covering longer audio content such as meetings, interviews, or calls. |

**Text-to-Speech — Evaluation Tasks**

| Task | What It Evaluates |
| --- | --- |
| TTS Quality | Evaluates the naturalness, clarity, and intelligibility of synthesized speech output. Typically assessed using Mean Opinion Score (MOS) or automated metrics that approximate human perceptual quality. |

#### Dataset, Inferencing Settings, Hardware Configuration

- **Dataset**: Select from the Dataset Library (e.g., MMLU, HellaSwag for LLMs; DrawBench, COCO for diffusion/image models) or a Custom Dataset aligned to the chosen Evaluation Task.
- **Inferencing Settings**: Configure generation parameters for the single model being evaluated — Max Tokens, Temperature, System Prompt, Top-P, Frequency/Presence Penalty, and other task-relevant settings. See \[Inferencing Settings (Hardware Benchmarking)\]
- **Hardware Configuration**: Select GPU Type (H100 or L40S) and GPU Count Per Node to run the evaluation on. See \[Hardware Configuration (Model Benchmarking)\](#step-5--hardware-configuration) for full details.

<Frame>
  ![Image](/images/image-61.png)
</Frame>

Once the Benchmarking is completed and status is Success, following metrices are generated for Evaluation.

<Frame>
  ![Image](/images/image-62.png)
</Frame>

#### Metrices for Model Evaluation

Result Navigation Tabs

| Tab | Description |
| --- | --- |
| Overview | Aggregated results across all datasets and tasks used in the evaluation run. Provides a single summary view of the model's overall performance. |
| \[Dataset Name\](e.g., MMLU) | Dataset-specific results showing metrics computed only against that dataset. For runs using multiple datasets, each dataset gets its own tab. |

**1. Task Metrics**

Task Metrics measure how accurately and correctly the model completed the evaluation task against the selected dataset. These are the primary quality indicators for the chosen Evaluation Task (e.g., Question Answering, Visual QA, Summarization).

| Metric | Description | Ideal Value |
| --- | --- | --- |
| F1 | The harmonic mean of Precision and Recall. Measures how well the model's answers overlap with the reference answers — balancing false positives (incorrect content generated) and false negatives (correct content missed). A score of \`1.0\` is perfect; \`0\` means no overlap. | Higher is better (max 1.0) |
| AnIs | Average Normalized Inverse Score — a normalized measure of answer quality that accounts for variations in phrasing. Useful for tasks where partial credit should be given for near-correct answers. | Higher is better |
| Accuracy | The percentage of evaluation samples for which the model's output exactly or substantially matches the correct answer. Computed as: \`Correct predictions / Total predictions\`. | Higher is better (max 1.0 / 100%) |
| Exact Match | The strictest accuracy metric — the proportion of samples where the model's output is a character-for-character match with the reference answer, with no tolerance for minor phrasing differences. Used heavily in QA benchmarks like SQuAD and MMLU. | Higher is better (max 1.0) |
| Choice Accuracy | For multiple-choice tasks (e.g., MMLU), the percentage of questions where the model selected the correct answer option (A, B, C, D). More lenient than Exact Match — scores the answer choice, not the full text. | Higher is better (max 1.0) |
| Avg Latency Ms | The average time (in milliseconds) the model took to generate a response per sample across the entire evaluation dataset. Included in Task Metrics as a quick latency reference alongside quality scores. | Lower is better |

**2. Performance Metrics**

Performance Metrics measure the speed and throughput characteristics of the model under the evaluation workload — critical for understanding whether the model can meet latency SLAs in production.

| Metric | Unit | Description | Ideal Value |
| --- | --- | --- | --- |
| Avg Latency Ms | ms | The mean inference latency across all evaluation samples — average time from request submission to response completion. | Lower is better |
| Max Latency Ms | ms | The maximum observed latency across all samples — the worst-case response time seen during the evaluation run. | Lower is better |
| Min Latency Ms | ms | The minimum observed latency — the fastest response time seen across all samples. | Lower is better |
| P50 Latency Ms | ms | The 50th percentile (median) latency — half of all requests were completed faster than this value. A robust central-tendency measure less affected by extreme outliers than the mean. | Lower is better |
| P95 Latency Ms | ms | The 95th percentile latency — 95% of requests completed within this time. A key SLA metric for real-world applications where tail latency matters. | Lower is better |
| P99 Latency Ms | ms | The 99th percentile latency — 99% of requests completed within this time. Reflects worst-case performance for the vast majority of traffic; critical for latency-sensitive production workloads. | Lower is better |
| Latency Std Dev Ms | ms | Standard deviation of latency across all samples — measures how consistent (or variable) the model's response times are. A high standard deviation indicates unpredictable latency spikes. | Lower is better |
| Total Tokens Input | count | The total number of input tokens processed across all evaluation samples (e.g., \`1,200\`). Useful for cost estimation and understanding the total prompt volume the model handled. | Reference value |
| Avg Tokens Per \[Request\] | count | The average number of input tokens per evaluation sample — reflects the typical prompt length for this dataset and task. | Reference value |
| Total Tokens Generated | count | The total number of output tokens generated by the model across all samples (e.g., \`100\`). | Reference value |
| Avg Input Length Tokens | count | Average number of tokens per input prompt across the dataset (e.g., \`12\`). Indicates how concise or detailed the evaluation prompts were. | Reference value |
| Avg Output Length Tokens | count | Average number of tokens per model response across the dataset (e.g., \`1\`). For choice-based tasks like MMLU, this is typically very small (1–3 tokens). | Reference value |

**3. Reliability Metrics**

Reliability Metrics measure the stability and robustness of the model's responses across the full evaluation dataset — indicating how dependable the model is when handling a large volume of diverse inputs.

| Metric | Unit | Description | Ideal Value |
| --- | --- | --- | --- |
| Error Rate | % | The percentage of evaluation samples for which the model returned an error — failed requests, timeouts, or malformed responses — rather than a valid output (e.g., \`100.00%\` means all requests failed; \`0%\` means none failed). | Lower is better (target: 0%) |
| Timeout Rate | % | The percentage of requests that exceeded the configured response timeout threshold and were terminated before a response was produced. | Lower is better (target: 0%) |
| Response Rate | % | The percentage of evaluation samples for which the model returned any response (successful or otherwise). Complement of Error Rate: \`Response Rate \+ Error Rate = 100%\` when no timeouts occur. | Higher is better (target: 100%) |
| Total Samples | count | The total number of evaluation samples in the dataset that were submitted to the model (e.g., \`100\`). Confirms the full evaluation scope. | Reference value |
| Successful Samples | count | The number of samples for which the model returned a valid, parseable response (e.g., \`0\` in a failed run). | Higher is better (target: Total Samples) |
| Empty Response Rate | % | The percentage of requests for which the model returned an empty or null response — even when no error was technically raised (e.g., \`100.00%\` means all responses were empty). A high Empty Response Rate alongside a high Error Rate typically indicates an authentication, access, or model configuration issue. | Lower is better (target: 0%) |

<Note>
  **Interpreting Reliability Metrics**: An `Error Rate` of 100% combined with an `Empty Response Rate` of 100% and `Successful Samples` of 0 indicates that none of the model's requests completed successfully. This typically points to an infrastructure or authentication issue (e.g., restricted model access, invalid credentials, network failure) rather than a model quality issue. Check the benchmarking run's logs for specific error messages
</Note>

**4. Output Quality**

Output Quality metrics evaluate characteristics of the model's generated text beyond simple accuracy — measuring safety, diversity, consistency, and language richness of the responses across the full dataset.

| Metric | Unit | Description | Ideal Value |
| --- | --- | --- | --- |
| Toxicity Score | 0–1 | A measure of how much harmful, offensive, or inappropriate content appears in the model's outputs, scored by an automated toxicity classifier. A score of \`0\` means no toxic content was detected; \`1\` means all outputs were flagged as toxic. | Lower is better (target: 0) |
| Repetition Score | 0–1 | Measures how often the model repeats the same phrases, sentences, or token sequences within or across its responses. High repetition indicates degenerate output patterns (e.g., the model getting "stuck" repeating content). | Lower is better (target: 0) |
| Hallucination Rate | % | The estimated percentage of responses containing factually incorrect or fabricated information not grounded in the prompt or dataset context. Measured by automated fact-checking or consistency checking against the dataset's reference answers. | Lower is better (target: 0%) |
| Std Dev Output Length | tokens | Standard deviation of output length in tokens across all responses — measures consistency of response length. A high value means the model produces wildly variable-length outputs; a low value means responses are consistently sized. | Lower is better for consistency |
| Vocabulary Richness (TTR) | 0–1 | Type-Token Ratio — the ratio of unique tokens to total tokens across all generated responses. Higher TTR means the model uses a diverse, rich vocabulary; lower TTR indicates repetitive or formulaic language patterns. | Higher is better for creative/generative tasks; lower may be acceptable for structured QA |
| Avg Output Length Tokens | tokens | Average number of tokens across all model responses (e.g., \`1\`). A very low average (1–2 tokens) is expected for multiple-choice tasks; higher values are expected for summarization, generation, or reasoning tasks. | Task-dependent |
| Max Output Length Tokens | tokens | The longest response generated across all evaluation samples (e.g., \`1\`). Indicates the upper bound of model verbosity for this dataset and configuration. | Task-dependent |
| Min Output Length Tokens | tokens | The shortest response generated across all evaluation samples (e.g., \`1\`). Along with Max and Avg, forms the complete output length distribution. | Task-dependent |

## Knowledge Base Benchmarking

**Knowledge Base Pipeline** benchmarking evaluates how well one or two models perform when paired with a connected Knowledge Base (RAG pipeline). Instead of testing a model's raw capability in isolation, this benchmarking type tests it in context — how effectively the model uses content retrieved from the Knowledge Base to produce accurate, grounded outputs — before that pipeline is relied on in production.

**What Knowledge Base Pipeline Benchmarking Helps Users Do**

- **Validate a RAG Pipeline End-to-End**: Test the full retrieval \+ generation flow against a real Knowledge Base, rather than evaluating the model or the retrieval step in isolation.
- **Compare Models on the Same Knowledge Base**: Run two different models against the identical Knowledge Base and dataset to see which one produces better, more grounded responses.
- **Catch Retrieval and Grounding Issues Early**: Surface problems like poor context retrieval or weak grounding before the pipeline is exposed to end users.
- **Reuse an Existing Knowledge Base:** Point the benchmark directly at any Knowledge Base already created in Q0 (see \[Knowledge Base\](/knowledge-base) documentation), without needing to rebuild it for testing purposes

#### Basic Details

Provide a unique **Benchmarking Name** (e.g., `Bench_Hardware`) and an optional **Description** (up to 500 characters), then click Continue.

<Frame>
  ![Image](/images/image-63.png)
</Frame>

#### Benchmarking Type

<Frame>
  ![Image](/images/image-64.png)
</Frame>

<Frame>
  ![Image](/images/image-65.png)
</Frame>

**Configuring Model 1 (and Model 2)**

| Field | Description |
| --- | --- |
| Model Source | Where the model comes from — e.g., \`Huggingface\` for Model 1, \`GCP\` for Model 2. Each model can be sourced independently. |
| Model Path | The repo path (Huggingface) or storage URI (GCP/cloud) where the model is located. Verified automatically with a green \*\*Verified\*\* badge once confirmed. |
| Model Class | The model architecture/pipeline class used to load the model (e.g., \`WhisperForConditionalGeneration\`). Must match the model's actual implementation. |
| Secret | Authentication method — \*\*Existing Secret\*\* (select a previously saved credential, e.g., \`Hugg\`, \`GCPtest\`) or \*\*Create New Secret\*\*. |
| Select Secret | The specific saved secret to use, scoped to the chosen Model Source. |

<Note>
  Configuring **Model 2** is optional for Knowledge Base Pipeline benchmarking. Configure only **Model 1** to evaluate a single model's performance against the Knowledge Base, or configure both to run a side-by-side comparison of two models against the same Knowledge Base and dataset — mirroring how \[Model Benchmarking\](#step-2--benchmarking-type) compares two models directly.
</Note>

#### Dataset

Select the dataset of queries/prompts that will be sent through the Knowledge Base pipeline for both retrieval and generation.

| Field | Description |
| --- | --- |
| Existing Dataset / Create New Dataset | Toggle between reusing a dataset already in Q0 (library or custom) or creating a new one specifically for this run. See \[Step 3 — Dataset\](#step-3--dataset) for full field details. |
| Select An Existing Dataset | Dropdown to choose the dataset once \*\*Existing Dataset\*\* is selected. |
| Add Another Dataset | Adds an additional dataset to the run, letting the Knowledge Base pipeline be evaluated against more than one dataset (e.g., different query sets or topics) in a single benchmarking run — each will appear as its own results tab once the run completes. |

#### Inferencing Settings

This step configures how each model generates its response after the Knowledge Base pipeline retrieves relevant context. Metrics are automatically enabled based on the benchmarking goal, and settings are configured independently for **Model 1** and **Model 2** (if configured).

<Frame>
  ![Image](/images/image-66.png)
</Frame>

<Note>
  Fields in this step change based on the model selected in Step 2. The Inferencing Settings shown are not fixed — Q0 detects the model's category/class (LLM, Diffusion Model, Vision-Language Model, ASR, Text-to-Speech) and surfaces only the relevant generation controls for that model type. The table below shows both the fields common across model types and examples of category-specific fields
</Note>

#### Hardware Configuration

<Frame>
  ![Image](/images/image-67.png)
</Frame>

Click **Create Benchmarking** to launch the run.

<Frame>
  ![Image](/images/image-68.png)
</Frame>

Once submitted, a Knowledge Base Pipeline run progresses through the same \[Benchmarking Status Flow\](#benchmarking-status-flow) as every other benchmarking type — **Verifying Model → Model Deploy → Dataset Verify → Benchmarking → Benchmark Completed** — shown on its own detail page alongside Model 1 / Model 2 cards and run metadata.

**Result Navigation Tabs**

| Tab | Description |
| --- | --- |
| Overview | Aggregated results across all datasets used in the run. In this example it mirrors the single dataset tab, since only one dataset (\`QO Test1\`) was used. |
| \[Dataset Name (e.g., QO Test1) | Dataset-specific results, showing metrics computed only against that dataset's queries run through the Knowledge Base pipeline. |

**1. Quality Metrics**

Quality Metrics measure how accurate, grounded, and relevant each model's generated answers were, both in absolute terms and relative to the context retrieved from the Knowledge Base.

| Metric | Description |
| --- | --- |
| Bleu | Measures n-gram overlap between the model's generated answer and a reference answer. Commonly used for translation/generation quality; \`0\` here indicates no exact reference match on this dataset. |
| Rouge 1 | Unigram (single-word) overlap between generated and reference answers. |
| Rouge 2 | Bigram (two-word sequence) overlap between generated and reference answers — a stricter phrasing match than Rouge 1. |
| Rouge L | Longest Common Subsequence overlap between generated and reference answers — captures sentence-level structural similarity. |
| Token F1 | The harmonic mean of token-level precision and recall between the generated answer and the reference answer. |
| Exact Match | Whether the generated answer exactly matches the reference answer, character-for-character. |
| Context Recall | The proportion of information in the reference answer that is actually present in the retrieved context — measures whether the pipeline retrieved \*enough\* of the right information. |
| Context Precision | The proportion of the retrieved context that is actually relevant to answering the query — measures whether retrieval pulled in irrelevant/noisy chunks. |
| Faithfulness Score | Measures how well the generated answer is grounded in the retrieved context, rather than the model's own unsupported knowledge. Higher means the answer sticks more closely to what was actually retrieved. |
| Hallucination Rate | The percentage of generated answers containing claims not supported by the retrieved context. A high value on both models here signals the pipeline (dataset, chunking, or retrieval quality) needs attention — see the interpretation note below. |
| Answer Length Ratio | Ratio of the generated answer's length to the reference answer's length — flags answers that are unusually short or long relative to expectations. |
| Deepeval Exact Match | Exact-match scoring computed via the DeepEval evaluation framework, used as a cross-check against Q0's native Exact Match metric. |
| Answer Relevance Score | Measures how directly the generated answer addresses the actual query asked, independent of whether it's grounded in context. Higher is better. |
| Answer Contains Expected | Whether the expected/reference answer's key content is contained within the model's generated answer. |
| Expected Contains Answer | The inverse check — whether the model's generated answer's content is fully contained within the expected/reference answer (flags overly narrow answers). |

<Note>
  Reading a low Faithfulness Score alongside a high Hallucination Rate:\*\* In this example, both models show a low Faithfulness Score (0.11–0.14) and a 60% Hallucination Rate. Together, these indicate the generated answers are not staying well-grounded in the content retrieved from the Knowledge Base — worth investigating the Knowledge Base's chunking strategy, embedding model, or dataset query difficulty (see \[Knowledge Base\](/knowledge-base) documentation) before treating either model as production-ready on this pipeline.
</Note>

**2. Retrieval Metrics**

Retrieval Metrics isolate the performance of the Knowledge Base's retrieval step — independent of the generation model — showing how well relevant context was found and how quickly.

| Metric | Description |
| --- | --- |
| Retrieval Mrr\*\* (Mean Reciprocal Rank) | Measures how high up the correct/relevant context chunk ranked in the retrieval results, averaged across all queries. Higher is better. |
| Avg Context Tokens | Average number of tokens in the retrieved context passed to the model per query. Equal across models — confirms both were given identical retrieved context for a fair comparison. |
| Avg Retrieval Score | The average similarity/relevance score of retrieved chunks against the query, as computed by the Knowledge Base's vector search. |
| Top1 Retrieval Score | The relevance score of the single highest-ranked retrieved chunk, averaged across all queries. |
| Retrieval Context Hit | Whether the correct/expected context chunk was present anywhere in the retrieved results for a query. |
| Retrieval Success Rate | Percentage of queries for which the retrieval step successfully returned at least one context chunk. |
| Avg Retrieval Latency | Average time taken by the Knowledge Base to retrieve relevant context per query. Since retrieval uses the same pipeline for both models, small differences typically reflect measurement noise rather than a true retrieval difference. |
| P50 Retrieval Latency Ms | Median retrieval latency across all queries. |
| P95 Retrieval Latency Ms | 95th percentile retrieval latency — reflects near-worst-case retrieval time. |
| Avg Retrieved Context Count | Average number of context chunks retrieved and passed to the model per query. |

**3. Performance Metrics**

Performance Metrics combine retrieval and generation timing into end-to-end latency and throughput figures for the full RAG pipeline.

| Metric | Description |
| --- | --- |
| Ttft Ms (Time to First Token) | Time from query submission to the first output token, including retrieval time. |
| Concurrency | The number of simultaneous requests simulated during the benchmark, as configured for the run. |
| Samples Per Second | The rate at which queries were fully processed (retrieval \+ generation) per second. |
| Total Tokens Input | Total input tokens (query \+ retrieved context) processed across the full run. |
| Requests Per Minute | Sustained end-to-end request throughput for the pipeline. |
| Avg Total Latency Ms | Average end-to-end latency per query — retrieval time plus generation time combined. |
| P50 Total Latency Ms | Median end-to-end latency across all queries. |
| P95 Total Latency Ms | 95th percentile end-to-end latency. |
| P99 Total Latency Ms | 99th percentile end-to-end latency — worst-case tail latency for the full pipeline. |
| Avg Tokens Per Second | Average output token generation rate across all queries. |
| Total Tokens Generated | Total output tokens generated by the model across the full run. |
| Avg Input Length Tokens | Average number of tokens in the raw query (before context is added), per request. |
| Total Wall Time Seconds | Total wall-clock time to complete the entire benchmark run. |
| Avg Output Length Tokens | Average number of tokens in the model's generated answers. |
| Avg Generation Latency Ms | Average time spent specifically on answer generation, excluding retrieval time. |
| Max Generation Latency Ms | The longest generation time observed for a single query. |
| Min Generation Latency Ms | The shortest generation time observed for a single query. |
| P50 Generation Latency Ms | Median generation-only latency. |
| P95 Generation Latency Ms | 95th percentile generation-only latency. |
| P99 Generation Latency Ms | 99th percentile generation-only latency. |
| Retrieval Latency Fraction | The proportion of total end-to-end latency attributable to retrieval, on a 0–1 scale. \`0.00\` indicates retrieval time is negligible relative to generation time for this run. |
| Generation Latency Fraction | The proportion of total end-to-end latency attributable to generation. \`1.00\` confirms generation dominates total latency, consistent with the near-zero Retrieval Latency Fraction above. |
| Generation Latency Std Dev Ms | Standard deviation of generation latency across all queries — measures consistency of generation speed. Lower is more predictable. |
| Avg Latency Per Output Token Ms | Average time taken to generate each individual output token — a normalized speed metric independent of total response length. |

<Tip>
  **Retrieval vs. Generation Latency Fraction**:These two fields always sum to `1.00` and show where end-to-end latency is actually being spent. In this run, retrieval accounts for effectively none of the total latency `0.00`) while generation accounts for all of it `1.00`) — meaning any latency optimization effort should focus on the generation model or its inference settings, not the Knowledge Base's retrieval step.
</Tip>

**4. Reliability Metrics**

| Metric | Description |
| --- | --- |
| Error Rate | Percentage of queries that returned an error anywhere in the pipeline (retrieval or generation). |
| Timeout Rate | Percentage of queries that exceeded the configured timeout threshold. |
| Response Rate | Percentage of queries that returned any response, successful or otherwise. |
| Total Samples | Total number of queries submitted to the pipeline during the run. |
| Successful Samples | Number of queries that completed successfully end-to-end. |
| Empty Response Rate | Percentage of queries for which the model returned an empty or null answer. |
| Retrieval Error Rate | Percentage of queries for which the retrieval step specifically failed. |
| Generation Error Rate | Percentage of queries for which the generation step specifically failed. |
| Retrieval Successful Samples | Number of queries for which retrieval completed successfully. |
| Generation Successful Samples | Number of queries for which answer generation completed successfully. |

<Note>
  A fully reliable run `100%` Response Rate, `0%` Error/Timeout/Empty Response rates across both retrieval and generation stages) is what's shown here — all 10 samples succeeded end-to-end for both models. This confirms the Quality and Faithfulness scores above reflect genuine model/pipeline behavior, not artifacts of failed or empty responses
</Note>

**5. Output Quality**

| Metric | Description |
| --- | --- |
| Std Dev Output Length | Standard deviation of generated answer length (in tokens) across all queries — measures consistency of response length. |
| Avg Output Length Tokens | Average length of generated answers, in tokens. |
| Max Output Length Tokens | The longest generated answer observed across all queries. |
| \*Min Output Length Tokens | The shortest generated answer observed across all queries. |